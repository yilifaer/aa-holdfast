"""Optional integrations with other Alliance Auth apps.

Nothing in here is imported unless the other app is installed and asks for it,
so an install without them carries no weight and no import errors.
"""
